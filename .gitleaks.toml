title = "gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Known false positives: example/template files with intentionally empty or placeholder values, no real secrets."
paths = [
  '''\.env\.example$''',
]
